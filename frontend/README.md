# CSAT Survey Frontend (React + Vite)

## Setup
```bash
cp .env.example .env
npm install
npm run dev
```

Env:
- `VITE_API_BASE=http://localhost:4000`

Routes:
- `/survey` user flow (email → OTP → submit)
- `/admin` admin dashboard
